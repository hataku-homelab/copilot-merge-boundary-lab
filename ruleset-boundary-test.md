This is a harmless one-line note added to test the repository ruleset boundary behavior.

## Observed Ruleset/Merge Status

Merge is **blocked**: the PR is in draft state and has no approvals. Repository ruleset requirements (requiring human review) are enforced as expected. No rules were disabled, altered, or bypassed.
